# ==============================================================================
#  默认值配置 - 导出（全局）
# ==============================================================================

# 文件→Markdown 的生产默认由本节唯一拥有；请求级 CLI/GUI 选项可覆盖。
# 各格式 TOML 中的同名键仅供旧配置缺少/留空本节时的 GUI 迁移回退。

to_md_image_extraction_mode = "file"
to_md_ocr_placement_mode = "main_md"
