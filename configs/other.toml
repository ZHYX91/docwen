# ==============================================================================
#  默认值配置 - 其他文件类型
# ==============================================================================

to_md_keep_images = true
to_md_enable_ocr = false
# 迁移回退；生产默认见 export.toml
