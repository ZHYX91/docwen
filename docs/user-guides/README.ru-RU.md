# DocWen

<p align="center">
  <img src="https://raw.githubusercontent.com/ZHYX91/docwen/main/assets/icon.svg" alt="DocWen logo" width="120">
</p>

[English](https://github.com/ZHYX91/docwen/blob/main/README.md) · [简体中文](https://github.com/ZHYX91/docwen/blob/main/docs/user-guides/README.zh-CN.md) · [繁體中文](https://github.com/ZHYX91/docwen/blob/main/docs/user-guides/README.zh-TW.md) · [Deutsch](https://github.com/ZHYX91/docwen/blob/main/docs/user-guides/README.de-DE.md) · [Français](https://github.com/ZHYX91/docwen/blob/main/docs/user-guides/README.fr-FR.md) · [Español](https://github.com/ZHYX91/docwen/blob/main/docs/user-guides/README.es-ES.md) · [Português](https://github.com/ZHYX91/docwen/blob/main/docs/user-guides/README.pt-BR.md) · [Русский](https://github.com/ZHYX91/docwen/blob/main/docs/user-guides/README.ru-RU.md) · [日本語](https://github.com/ZHYX91/docwen/blob/main/docs/user-guides/README.ja-JP.md) · [한국어](https://github.com/ZHYX91/docwen/blob/main/docs/user-guides/README.ko-KR.md) · [Tiếng Việt](https://github.com/ZHYX91/docwen/blob/main/docs/user-guides/README.vi-VN.md)

Программное обеспечение для конвертации форматов документов и диаграмм - поддерживает двустороннюю конвертацию Word/Markdown/Excel. Работает локально, обеспечивая безопасность и надежность данных.

## 📖 История проекта

Это программное обеспечение изначально было разработано для повседневной работы печатного отдела для решения следующих проблем:
- Форматы документов, присылаемых различными отделами, хаотичны и требуют приведения к стандартным форматам.
- Существует множество типов документов, каждый из которых имеет свои фиксированные требования к формату.
- Требуется работа оффлайн, адаптация к внутренней сети и старому оборудованию.

**Философия дизайна**: Это программное обеспечение позиционируется как легкий и простой инструмент. Хотя оно не может сравниться с профессиональными инструментами, такими как LaTeX или Pandoc, по профессионализму и функциональной полноте, оно выигрывает за счет нулевой стоимости обучения и готовности к использованию, что делает его подходящим для повседневных офисных сценариев, где требования к формату не слишком строгие.

## ✨ Основные функции

- **📄 Конвертация формата документа** - Двусторонняя конвертация Word ↔ Markdown. Поддерживает конвертацию математических формул, двустороннее преобразование разделителей (три вида линий Markdown против разрывов страниц, разрывов разделов и горизонтальных линий Word), а также восстановление явных `<` / `^` marker в таблицах Markdown в прямоугольные объединения ячеек Word. Поддерживает форматы DOCX/DOC/WPS/RTF/ODT и другие.
- **📊 Конвертация формата таблиц** - Двусторонняя конвертация Excel ↔ Markdown. Поддерживает форматы XLSX/XLS/ET/ODS/CSV/TSV, настраиваемые стратегии экспорта объединенных ячеек (`fill / empty / marker`) и инструменты сводки таблиц. Шаблоны Markdown→XLSX снова поддерживают YAML-поля, вертикальные и горизонтальные заполнители столбцов; полное восстановление Excel-шаблонов, изображений и объединений остается целью паритета.
- **📑 PDF и файлы макетов** - Конвертация PDF/XPS/OFD в Markdown или DOCX. Поддерживает слияние, разделение PDF и другие операции.
- **🖼️ Обработка изображений** - Поддерживает двустороннюю конвертацию и сжатие форматов JPEG/PNG/GIF/BMP/TIFF/WebP/HEIC.
- **📥 Импорт других форматов** - Поддерживает одностороннюю конвертацию HTML/MHTML/ENEX/EPUB/PPTX/PPT в Markdown.
- **🔍 Распознавание текста OCR** - Интегрирован RapidOCR для извлечения текста из изображений и PDF.
- **✏️ Корректура текста** - Проверяет опечатки, пунктуацию, символы и чувствительные слова в файлах Word (.docx) и Markdown (.md) на основе пользовательских словарей. Правила можно редактировать в интерфейсе настроек.
- **📝 Система шаблонов** - Гибкий механизм шаблонов, поддерживающий пользовательские форматы документов и отчетов.
- **💻 Двухрежимная работа** - Графический интерфейс пользователя (GUI) + Интерфейс командной строки (CLI).
- **🔒 Локальная обработка с защитой исходящих соединений зависимостей** - Конвертация не зависит от онлайн-сервисов. Пока DocWen работает, процесс Python блокирует DNS и IPv4/IPv6 для внутренних зависимостей; внешние приложения Office сохраняют системную сетевую политику.
- **🔗 Работа в одном экземпляре** - Автоматически управляет экземплярами программы и поддерживает интеграцию с сопутствующим плагином Obsidian.

## 📸 Скриншоты

| Пакет | Markdown |
| --- | --- |
| ![Панель пакетной обработки](../assets/screenshots/batch-light.png) | ![Главное окно](../assets/screenshots/main-light.png) |

| Документ | Таблица |
| --- | --- |
| ![Панель документа](../assets/screenshots/conversion-document-light.png) | ![Панель таблиц](../assets/screenshots/conversion-spreadsheet-light.png) |

| Изображение | Файлы макетов |
| --- | --- |
| ![Панель изображений](../assets/screenshots/conversion-image-light.png) | ![Панель макета](../assets/screenshots/conversion-layout-light.png) |

Журнал изменений: см. [CHANGELOG.md](../CHANGELOG.md)

## 🚀 Быстрый старт

### Установка из исходников

**Требования**: Python 3.12

**Целевая граница 0.9**: Этот исходный код собирает пакеты для Windows x64 и Ubuntu 24.04 x64.
Другие дистрибутивы Linux и macOS остаются путями из исходников/для разработки и не входят в
обязательства пакета Ubuntu.

**Способ 1: С помощью uv (рекомендуется)**

Установите [uv](https://docs.astral.sh/uv/getting-started/), затем:

```bash
git clone https://github.com/ZHYX91/docwen.git
cd docwen
uv sync --frozen --all-extras
```

Исходный код, тесты и сборки DocWen 0.9 поддерживают только файл блокировки репозитория с `uv 0.12.0`; `pip install -e` не поддерживается.

### Запуск программы

Для упакованной версии Windows: дважды щелкните `DocWen.exe`, чтобы запустить GUI. После установки из исходников:

```bash
docwen-gui  # Режим GUI
docwen      # Режим CLI
```

### Примечания для macOS

**Текущее ограничение**: В macOS возможности `convert`, `validate`, `number`, `merge` и `split` сейчас
недоступны. Ниже описаны только необязательные зависимости для экспериментов при разработке.

**Поддержка LibreOffice (необязательно)**

Чтобы конвертировать старые форматы вроде `.doc` и `.xls`, установите LibreOffice:  
Скачать: https://www.libreoffice.org/download/

**Поддержка изображений HEIC (необязательно)**

Чтобы обрабатывать изображения HEIC/HEIF:

```bash
brew install libheif
pip install pillow-heif
```

### Требования для GUI на Linux

**Поддерживаемая пакетная цель**: DocWen 0.9 поддерживает GUI и CLI в пакете Ubuntu 24.04 x64.
Эти требования не распространяют поддержку на другие дистрибутивы или архитектуры.

- Установлено окружение рабочего стола (GNOME, KDE, XFCE и т.д.)
- GUI использует PySide6 (Qt6) и больше не зависит от Python Tk. Если запуск падает из-за отсутствующих системных библиотек, установите Qt runtime-зависимости, указанные в сообщении об ошибке (обычно это OpenGL/X11).
- Для headless-серверов лучше использовать CLI-вход `docwen` вместо GUI; в упакованных Windows-сборках также доступен `DocWenCLI.exe`.

### Руководство по быстрому старту

1.  **Подготовьте файл Markdown**:

    ```markdown
    ---
    Заголовок: Тестовый документ
    ---
    
    ## Тестовый заголовок
    
    Это содержимое тела теста.
    ```

2.  **Конвертация перетаскиванием**:
    - Запустите программу.
    - Перетащите файл `.md` в окно.
    - Выберите шаблон.
    - Нажмите "Конвертировать в DOCX".

3.  **Получение результатов**:
    - Стандартизированный документ Word будет сгенерирован в том же каталоге.

**Совет**: Вы можете использовать примеры файлов в каталоге `samples/` для быстрого ознакомления с функциями программы.

## 🖥️ Использование графического интерфейса

Большинство пользователей используют это программное обеспечение через графический интерфейс. Вот подробное руководство по эксплуатации.

### Обзор интерфейса

Программа использует **адаптивный трехколоночный макет**:

| Область | Описание | Время отображения |
| :--- | :--- | :--- |
| **Центральная колонка (Основная область)** | Область перетаскивания файлов, панель операций, строка состояния | Всегда отображается |
| **Правая колонка** | Выбор шаблона / Панель конвертации формата | Автоматически расширяется после выбора файла |
| **Левая колонка** | Список пакетных файлов (сгруппированных по типу) | Отображается при переключении в пакетный режим |

### Основной рабочий процесс

1.  **Запуск программы**: Дважды щелкните `DocWen.exe` (Windows пакет) или запустите `docwen-gui`.
2.  **Импорт файла**:
    -   Способ 1: Перетащите файлы прямо в окно.
    -   Способ 2: Нажмите кнопку "Добавить" в области перетаскивания, чтобы выбрать файлы.
3.  **Выбор шаблона** (если требуется конвертация): Правая панель шаблонов расширяется автоматически; выберите подходящий шаблон.
4.  **Настройка опций**: Отметьте необходимые опции конвертации/экспорта в панели операций.
5.  **Выполнение операции**: Нажмите соответствующую функциональную кнопку (например, "Экспорт MD", "Конвертировать в DOCX" и т.д.).
6.  **Просмотр результата**: Строка состояния показывает прогресс и результат; нажмите справа на действие «Открыть вывод», чтобы открыть место вывода.

### Одиночный режим vs Пакетный режим

Программа поддерживает два режима обработки, переключаемых кнопкой-переключателем в области перетаскивания файлов:

**Одиночный режим** (По умолчанию):
-   Обрабатывает один файл за раз.
-   Простой интерфейс, подходит для ежедневного использования.

**Пакетный режим**:
-   Импортирует несколько файлов одновременно.
-   Левая колонка показывает категоризированный список файлов (сгруппированный по документу/таблице/изображению и т.д.).
-   Поддерживает пакетное добавление, удаление и сортировку.
-   Нажатие на файл в списке переключает текущую цель операции.

### Функции панели операций

Панель операций автоматически настраивает доступные опции в зависимости от типа файла:

| Тип файла | Доступные операции |
| :--- | :--- |
| Документ Word | Экспорт MD, Конвертировать PDF, Корректура текста, OCR |
| Markdown | Конвертировать DOCX, Конвертировать PDF, Корректура текста |
| Таблица Excel | Экспорт MD, Конвертировать PDF, Сводка таблицы |
| Файл PDF | Экспорт MD, Слияние, Разделение, OCR |
| Файл изображения | Конвертация формата, Сжатие, OCR |
| HTML/EPUB/PPTX и др. | Экспорт MD |

### Интерфейс настроек

Нажмите кнопку «Настройки» в заголовке рабочей области, чтобы открыть настройки:

Настройки организованы по вкладкам: **Общие**, **Текст**, **Проверка**, **Документ**, **Таблица**, **Изображение**, **Макет**, **Ссылка**, **Форматирование**, **Вывод**, **Экспорт**, **Лог**, **Другое**.

### Горячие клавиши

-   **Перетаскивание внешнего файла**: Перетащите прямо в окно для импорта.
-   **Открыть вывод**: Нажмите справа в строке состояния на действие «Открыть вывод», чтобы открыть место вывода.
-   **Щелчок правой кнопкой мыши по элементу шаблона**: Открыть расположение файла шаблона.

---

## 🔧 Использование CLI

Помимо графического интерфейса, DocWen предоставляет интерфейс командной строки (CLI) для автоматизации, пакетной обработки и внешних интеграций.

### Рекомендуемый поток для автоматизации

Для скриптов, агентов и плагинов рекомендуется следующий порядок:

1. `inspect <file> [--json]`: сначала определить реальную категорию файла, формат и поддерживаемые действия.
2. `schema convert`: получить машиночитаемый контракт и условные правила для `convert`.
3. `convert <file> --to <fmt> --output <path> --dry-run --json`: предварительно проверить распознавание, нормализацию и маршрутизацию без записи файлов.
4. `convert <file> --to <fmt> --output <path> ...`: затем выполнить реальное преобразование.

### Типовые примеры

```bash
# Windows-пакет
DocWenCLI.exe inspect document.docx --json

# Экспорт контракта convert для скриптов / агентов
DocWenCLI.exe schema convert

# Предпросмотр выполнения без записи результатов
DocWenCLI.exe convert report.docx --to md --output report.md --extract-img --ocr --dry-run --json

# Экспорт Word в Markdown (извлечение изображений + OCR)
DocWenCLI.exe convert report.docx --to md --output report.md --extract-img --ocr

# Markdown в Word (шаблон + режим объединения заголовка и текста)
DocWenCLI.exe convert document.md --to docx --output document.docx --template template.docx.402044547bbb3acc1c7b3469d495c788647b3bb23ffdcff5427730c8baa711f1 --heading-merge-mode punct_required

# Управление режимом изображений и размещением OCR-текста в Markdown
DocWenCLI.exe convert report.docx --to md --output report.md --extract-img --image-mode file --ocr --ocr-placement image_md

# Просмотр возможностей времени выполнения и ворот зависимостей
DocWenCLI.exe doctor --json
DocWenCLI.exe resources list formats --json

# Проверка документов
DocWenCLI.exe validate document.docx --check typo --check punct
DocWenCLI.exe validate input.md --check typo --check punct

# Из исходников / через uv
# inspect -> schema -> dry-run -> convert
# docwen inspect document.docx --json
# docwen schema convert
# docwen convert document.docx --to md --output document.md --dry-run --json
# docwen convert document.docx --to md --output document.md
```

### Часто используемые команды и параметры

Таблица ниже перечисляет только часто используемые команды. Для полного списка команд используйте `docwen --help` (исходники / uv) или `DocWenCLI --help` (пакетная версия).

| Команда / параметр | Описание |
| --- | --- |
| `convert <file> --to <fmt> --output <path>` | Единая точка входа для конвертации. |
| `convert <file> --to <fmt> --output <path> --dry-run --json` | Предварительно показывает распознавание, нормализацию, маршрутизацию и итоговые параметры без реального запуска конвертации. |
| `schema convert` | Экспортирует машиночитаемый контракт, значения по умолчанию, условия и канонические ключи для `convert`. |
| `validate <file> --check ...` | Проверка документов (`typo/punct/symbol/sensitive/all/none`). Используйте `--json` для оболочки CLI; `--report` — необязательный путь к файлу отчета. |
| `inspect <file> [--json]` | Показывает категорию/формат файла, рекомендуемые действия и предупреждения о расхождении расширения и содержимого. |
| `doctor --json` | Возвращает диагностику вместе со сводкой возможностей времени выполнения и информацией о зависимостях. |
| `resources list formats --json` | Показывает целевые форматы по категории источника и добавляет сводки о зависимостях времени выполнения и ограничениях. |
| `resources list templates` | Список доступных шаблонов. |
| `resources list numbering-schemes` | Список доступных схем нумерации. |
| `--template <id>` | Точный канонический ID из `resources list templates`; отображаемые имена, имена файлов и пути отклоняются. ID DOCX применяются к `docx/doc/odt/rtf/wps/pdf`, ID XLSX — к `xlsx/xls/ods/csv`. |
| `--extract-img` / `--no-extract-img` / `--ocr` | Извлечение изображений и OCR для `convert --to md`. |
| `--image-mode file|base64` | Управляет способом вывода изображений при экспорте в Markdown. |
| `--ocr-placement image_md|main_md` | Определяет, записывать OCR-текст в Markdown рядом с изображением или в основной Markdown-файл. |
| `--heading-merge-mode punct_required|always|never` | Управляет стратегией объединения «заголовок + текст» для `convert --to docx`. |
| `--optimization <id>` | Явно включает профиль оптимизации (см. `resources list optimizations`). |
| `batch convert|validate ... --jobs <n> [--continue-on-error]` | Параметры пакетной обработки. |
| `--json` / `--quiet` / `--timing` | Структурированный вывод, сокращенные логи и данные о времени для скриптов или плагинов. |

В режиме `punct_required` точный список по умолчанию: `。：！？.:!?`. Его можно изменить в настройках форматирования; пустое значение отключает объединение в этом режиме. Запятые, точки с запятой, перечислительные запятые, тире и многоточия по умолчанию исключены.


## 📝 Соглашения о синтаксисе Markdown

### Сопоставление уровней заголовков

Чтобы облегчить запоминание коллегам без базовых знаний, заголовки Markdown в этом программном обеспечении соответствуют заголовкам Word **один к одному**:
- Заголовок документа (title) и подзаголовок (subtitle) помещаются в метаданные YAML.
- Markdown `# Заголовок 1` соответствует Word "Заголовок 1".
- Markdown `## Заголовок 2` соответствует Word "Заголовок 2".
- И так далее, поддерживается до 9 уровней заголовков.

**Совет**: Если вы предпочитаете использовать заголовок первого уровня Markdown (`#`) как заголовок документа, начиная со второго уровня (`##`) для подзаголовков текста, вы можете настроить стиль «Заголовок 1» в шаблоне Word так, чтобы он выглядел как заголовок документа (например, выравнивание по центру, жирный шрифт, более крупный размер шрифта), и выбрать схему нумерации, которая пропускает нумерацию заголовков первого уровня в настройках. Таким образом, ваши заголовки первого уровня будут выглядеть как заголовки документа.

### Разрывы строк и абзацы

**Основное правило**: Каждая непустая строка по умолчанию обрабатывается как отдельный абзац.

**Смешанные абзацы**: Когда подзаголовок должен быть смешан с основным текстом в одном абзаце (режим по умолчанию: «Требуется пунктуация»), должны быть выполнены следующие условия:
1.  Подзаголовок заканчивается знаком препинания (поддерживаются многоязычные знаки препинания, включая точки, вопросительные знаки, восклицательные знаки и другие распространённые знаки завершения).
2.  Основной текст расположен на **следующей строке** после подзаголовка.
3.  Строка основного текста не может быть специальным элементом Markdown (таким как заголовки, блоки кода, таблицы, списки, цитаты, блоки формул, разделители и т.д.).

**Пример**:
```markdown
## I. Требования к работе.
Это совещание требует, чтобы все подразделения серьезно реализовали...
```
Вышеуказанные две строки будут объединены в один абзац, где "I. Требования к работе." сохраняет формат подзаголовка, а "Это совещание..." сохраняет формат основного текста.

**Примечание**:
- Между подзаголовком и основным текстом не должно быть пустой строки; в противном случае они будут распознаны как отдельные абзацы.
- По умолчанию (режим «Требуется пунктуация»), если подзаголовок не заканчивается завершающим знаком препинания, он не объединяется со следующей строкой даже без пустой строки.
- Это можно изменить в Настройки → Форматирование → «MarkDown в документ» → «Heading + body merge mode».

### Двусторонняя конвертация разделителей

Поддерживает двустороннюю конвертацию между разделителями Markdown и разрывами страниц/разрывами разделов/горизонтальными линиями Word:

-   **DOCX → MD**: Разрывы страниц, разрывы разделов и горизонтальные линии Word автоматически конвертируются в разделители Markdown.
-   **MD → DOCX**: Markdown `---`, `***`, `___` автоматически конвертируются в соответствующие элементы Word.
-   **Настраиваемость**: Конкретные отношения сопоставления могут быть настроены в интерфейсе настроек.

### Списки задач

Поддерживает двунаправленное преобразование списков задач GFM:

```markdown
- [ ] Задача
- [x] Выполнено
```

-   **MD → DOCX**: Отображается как маркированный список с текстовым префиксом `☐` / `☑`.
-   **DOCX → MD**: Преобразует элементы списка, начинающиеся с `☐` / `☑` / `☒`, обратно в `- [ ]` / `- [x]`.
-   **Примечание о шрифтах**: `☐`/`☑` могут не отображаться в некоторых шрифтах. При необходимости используйте шрифты, такие как «Segoe UI Symbol», в шаблоне Word.

### Встраивание изображений и размер

Поддерживает встраивание изображений в стиле Obsidian/Wiki и стандартного Markdown, с опциональным размером (px):

```markdown
![[image.png]]
![[image.png|300]]
![[image.png\|300]]
![alt](image.png =300x200)
![alt](image.png =300x)
![alt|300](image.png)
```

- Без размера: исходный размер, ограничен доступной шириной (страница/ячейка)
- С размером: увеличение разрешено, но ширина всё равно ограничена
- Абзац только с изображением: использует стиль абзаца “Image” (по центру, одинарный интервал)

### Обработка ссылок

Поддерживает кликабельные ссылки при Markdown -> DOCX:

```markdown
[Docwen](https://example.com)
[[Target]]
[[Target|Open target]]
<https://example.com>
<user@example.com>
```

- Markdown-ссылки и Wiki-ссылки по умолчанию записываются как гиперссылки Word
- Wiki-ссылки преобразуются в локальные ссылки `file:///`, если целевой файл найден
- Автоссылки в угловых скобках поддерживают `https://...` и адреса `mailto:...`
- Автоматическое преобразование необрамлённых URL применяется отдельно к каждому запросу Markdown -> DOCX, по умолчанию отключено и включается параметром `[non_embed_links].auto_link_bare_url` в `configs/link.toml`
- Markdown -> XLSX не создает DOCX-плейсхолдеры гиперссылок и сохраняет исходный синтаксис ссылок

## 📖 Подробное руководство по использованию

### Word в Markdown

1.  Перетащите файл `.docx` в окно программы.
2.  Программа автоматически анализирует структуру документа.
3.  Генерирует файл `.md`, содержащий метаданные YAML.

**Поддерживаемые форматы**:
-   `.docx` - Стандартный документ Word.
-   `.doc` - Автоматически конвертируется в DOCX для обработки.
-   `.wps` - Документ WPS автоматически конвертируется.

**Опции экспорта**:

| Опция | Описание |
| :--- | :--- |
| **Извлечь изображения** | Если отмечено, изображения в документе извлекаются в выходную папку, а ссылки на изображения вставляются в файл MD. |
| **OCR изображений** | Если отмечено, выполняет OCR на изображениях и создает файл изображения .md (содержащий распознанный текст). |
| **Расширенная оптимизация полей** | Если отмечено, извлекаются более насыщенные структурированные метаданные; в противном случае используется упрощённый режим только с заголовком и подзаголовком. |
| **Очистить номера подзаг.** | Если отмечено, удаляет номера перед подзаголовками (например, "一、", "（一）", "1." и т.д.) и конвертирует их в чистый текст заголовка. |
| **Добавить номера подзаг.** | Если отмечено, автоматически добавляет номера на основе уровней заголовков (схему нумерации можно настроить в настройках). |

Примечание: DOCX -> MD теперь также восстанавливает многоуровневую нумерацию, связанную в numbering.xml через стили абзаца (pStyle). Поэтому префиксы заголовков, созданные многоуровневыми списками Word/WPS, такие как "一、", "（一）", "1．", "（1）" и "①", сохраняются и в упрощённом режиме, и в режиме расширенной оптимизации полей; уровень заголовка по-прежнему определяется корректно при включённой опции "Очистить номера подзаг.".

### Markdown в Word

1.  Подготовьте файл `.md` с заголовком YAML.
2.  Перетащите его в окно программы и выберите соответствующий шаблон Word.
3.  Программа автоматически заполняет шаблон и генерирует документ.

**Опции конвертации**:

| Опция | Описание |
| :--- | :--- |
| **Очистить номера подзаг.** | Если отмечено, удаляет номера перед подзаголовками. |
| **Добавить номера подзаг.** | Если отмечено, автоматически добавляет номера на основе уровней заголовков. |

**Примечание**: Если в документе есть абзацы, где подзаголовки и основной текст смешаны, в файле MD должны соблюдаться строгие разрывы строк (см. "Разрывы строк и абзацы" выше).

### Автоматическая обработка стилей шаблона

Конвертер автоматически обнаруживает и обрабатывает стили шаблона во время конвертации Markdown → DOCX:

#### Классификация стилей

**Стиль абзаца**: Применяется ко всему абзацу.

| Стиль | Поведение обнаружения | Внедрение при отсутствии | Источник |
| :--- | :--- | :--- | :--- |
| Заголовок (1~9) | Обнаруживает стиль абзаца | Стили заголовков шаблона | Встроенный Word |
| Блок кода | Обнаруживает стиль абзаца | Шрифт Consolas + Серый фон | Определено ПО |
| Цитата (1~9) | Обнаруживает стиль абзаца | Серый фон + Левая граница | Определено ПО |
| Блок формулы | Обнаруживает стиль абзаца | Специфический стиль формулы | Определено ПО |
| Разделитель (1~3) | Обнаруживает стиль абзаца | Стиль абзаца нижней границы | Определено ПО |

**Стиль символа**: Применяется к выбранному тексту.

| Стиль | Поведение обнаружения | Внедрение при отсутствии | Источник |
| :--- | :--- | :--- | :--- |
| Встроенный код | Обнаруживает стиль символа | Шрифт Consolas + Серое затенение | Определено ПО |
| Встроенная формула | Обнаруживает стиль символа | Специфический стиль формулы | Определено ПО |

**Стиль таблицы**: Применяется ко всей таблице.

| Стиль | Поведение обнаружения | Внедрение при отсутствии | Источник |
| :--- | :--- | :--- | :--- |
| Трехлинейная таблица | Приоритет конфиг. польз. | Определение стиля трехлинейной таблицы | Определено ПО |
| Таблица-сетка | Приоритет конфиг. польз. | Определение стиля таблицы-сетки | Определено ПО |

**Определение нумерации**: Используется для форматов списков.

| Тип | Поведение обнаружения | Обработка при отсутствии |
| :--- | :--- | :--- |
| Нумерация списка | Сканирует существующие определения упорядоченных/неупорядоченных списков в шаблоне | Использует предустановку decimal/bullet |

#### Интернационализация имен стилей

-   **Встроенные стили Word** (heading 1~9):
    -   Имена стилей используют стандартные английские имена Word (например, `heading 1`).
    -   Word автоматически отображает локализованные имена в зависимости от языка системы (например, "Заголовок 1" в русской системе).
-   **Стили, определенные ПО** (Блок кода, Цитата, Формула, Разделитель, Таблица и т.д.):
    -   Внедряет соответствующие имена стилей языка в зависимости от настройки языка интерфейса ПО.
    -   Китайский интерфейс: Внедряет "代码块", "引用 1", "三线表" и т.д.
    -   Английский интерфейс: Внедряет "Code Block", "Quote 1", "Three Line Table" и т.д.

**Предложение**: После настройки стилей в шаблоне конвертер автоматически будет использовать ваши стили; если они отсутствуют в шаблоне, он будет использовать встроенные предустановленные стили.

### Обработка файлов таблиц

1.  **Excel/CSV в Markdown**: Перетащите файлы `.xlsx` или `.csv` для автоматической конвертации в таблицы Markdown.
2.  **Markdown в Excel**: Таблицы Markdown можно экспортировать в XLSX. Шаблоны поддерживают YAML-поля, заполнители столбцов и изображений, а также объединенные или защищенные ячейки.

**Поддерживаемые форматы**:
-   `.xlsx` - Стандартный документ Excel.
-   `.xls` - Автоматически конвертируется в XLSX для обработки.
-   `.et` - Таблица WPS автоматически конвертируется.
-   `.csv` - Текстовая таблица CSV.
-   `.tsv` - Текстовая таблица TSV (с разделителями-табуляциями).


### Функция корректуры текста

Программа предоставляет четыре настраиваемых правила корректуры:

1.  **Проверка парности пунктуации** - Обнаруживает, совпадают ли парные знаки препинания, такие как скобки и кавычки.
2.  **Коррекция символов** - Обнаруживает смешанное использование китайской и английской пунктуации.
3.  **Проверка опечаток** - Проверяет распространенные опечатки на основе пользовательского словаря.
4.  **Обнаружение чувствительных слов** - Обнаруживает чувствительные слова на основе пользовательского словаря.

**Пользовательские словари**: Визуально редактируйте словари опечаток и чувствительных слов в интерфейсе "Настройки".

**Использование**:
1.  Перетащите документ Word или файл Markdown для проверки в программу.
2.  Отметьте необходимые правила корректуры.
3.  Нажмите кнопку "Корректура текста".
4.  Результаты корректуры отображаются в виде комментариев в документе. Для файлов Markdown результаты выводятся в формате JSON-отчёта.

Примечание (JSON-отчёт для корректуры Markdown):
- Движок: `text_rules` + Markdown-адаптер `md_spell`
- Вывод: текущий CLI-вход для проверки — `validate`; используйте `--json` для оболочки CLI. `--report` — необязательный путь к файлу отчета.

- Отличается от `--json` (JSON-оболочка CLI)

## 🛠️ Система шаблонов

### Использование существующих шаблонов

Программа поставляется с различными шаблонами, включая многоязычные версии. Вы можете выбирать и использовать их по мере необходимости. Файлы шаблонов находятся в каталоге `templates/`.

### Пользовательские шаблоны

1.  Создайте файл шаблона с помощью Word или WPS.
2.  Обратитесь к существующим шаблонам и вставьте заполнители, такие как `{{Title}}` и т.д., где требуется заполнение.
3.  В шаблоне встроенные стили Заголовок 1 ~ Заголовок 5 должны быть изменены вручную.
4.  Сохраните шаблон в каталог `templates/`.
5.  Перезапустите программу, и новый шаблон будет загружен автоматически.

Вы также можете скопировать существующий шаблон, изменить его и переименовать.

### Использование заполнителей

#### Заполнители шаблона Word

**Заполнители полей YAML**: Используйте формат `{{ИмяПоля}}` в шаблоне, который будет заменен соответствующим значением в заголовке YAML файла Markdown во время конвертации.

| Заполнитель | Описание |
| :--- | :--- |
| `{{Заголовок}}` | Заголовок документа (Правила извлечения см. ниже) |
| `{{Тело}}` | Позиция вставки содержимого тела Markdown |
| Другие | Поддерживает любое пользовательское поле |

**Приоритет получения заголовка**:

| Приоритет | Источник | Описание |
| :--- | :--- | :--- |
| 1 | Поле YAML `Title` | Наивысший приоритет |
| 2 | Поле YAML `aliases` | Берет первый элемент списка или строковое значение |
| 3 | Имя файла | Имя файла без расширения `.md` |

**Многоязычная поддержка**: Заполнители заголовка и тела поддерживают несколько языков, например: заголовок может быть `{{Заголовок}}`, `{{title}}`, `{{标题}}` и т.д., тело может быть `{{Тело}}`, `{{body}}`, `{{正文}}` и т.д.

#### Заполнители шаблона Excel (цель паритета со старой версией)

XLSX-шаблоны поддерживают YAML-поля, вертикальные `{{↓поле}}` и горизонтальные `{{→поле}}` заполнители столбцов, заполнители изображений, а также объединенные или защищенные ячейки.

**1. Заполнитель поля YAML** `{{ИмяПоля}}`

Используется для заполнения одного значения из заголовка YAML файла Markdown:

```markdown
---
ReportName: Статистика годовых продаж 2024
Unit: Отдел продаж
---
```

`{{ReportName}}`, `{{Unit}}` в шаблоне будут заменены соответствующими значениями. Поле заголовка также следует правилам приоритета.

**2. Заполнитель заполнения столбца** `{{↓ИмяПоля}}`

Извлекает данные из таблицы Markdown и заполняет **вниз** строку за строкой, начиная с позиции заполнителя:

```markdown
| ИмяПродукта | Количество |
|:--- |:--- |
| Продукт A | 100 |
| Продукт B | 200 |
```

`{{↓ИмяПродукта}}` в шаблоне Excel будет заменен на "Продукт A", а следующая строка будет заполнена "Продукт B".

**3. Заполнитель заполнения строки** `{{→ИмяПоля}}`

Извлекает данные из таблицы Markdown и заполняет **вправо** столбец за столбцом, начиная с позиции заполнителя:

```markdown
| Месяц |
|:--- |
| Янв |
| Фев |
| Мар |
```

`{{→Месяц}}` в шаблоне Excel будет заполнен "Янв", "Фев", "Мар" последовательно вправо.

**Обработка объединенных ячеек**:

- Markdown -> Excel продолжает сохранять исходные merged ranges шаблона.
- В известных шаблонных областях столбцового типа, состоящих из непрерывных плейсхолдеров `{{↓ИмяПоля}}`, программа может восстанавливать прямоугольные объединения по явным маркерам `<` / `^` из Markdown-таблиц.
- В распознавании объединений участвуют только ячейки, чей текст после обрезки пробелов точно равен `<` или `^`; `\<` и `\^` сохраняются как буквальный текст.
- Невалидные прямоугольники или конфликты с существующими merged ranges шаблона понижаются до обычного текста с предупреждением вместо принудительного перезаписывания структуры шаблона.

**Слияние данных нескольких таблиц**: Если в Markdown есть несколько таблиц, использующих одно и то же имя заголовка, данные будут объединены по порядку и заполнены последовательно.

## 🔌 Плагин Obsidian

Сопутствующий плагин Obsidian опубликован отдельно и работает вместе с конвертером:

### Основные характеристики

-   **🚀 Запуск в один клик** - Значок на боковой панели для быстрого запуска конвертера.
-   **📂 Автоматическая передача** - Автоматически передает путь к текущему открытому файлу.
-   **🔄 Управление одним экземпляром** - Автоматически отправляет файл, если программа уже запущена, перезапуск не требуется.
-   **🔒 Ограниченное локальное управление** - Использует типизированные запросы `status`, `open` и `activate` без поиска процессов по имени и без файлов команд или состояния.

### Принцип работы

Транспорт runtime/control в DocWen Core может использовать именованный канал Windows либо сокет AF_UNIX
в Linux/macOS. Файловая блокировка отвечает только за владение единственным экземпляром; файлы не
используются для передачи команд управления. Это описание возможности Core. DocWen Assistant 2.0
остаётся только для Windows desktop и не имеет комбинированной приёмки в Linux/macOS.

1.  **Первый клик** → Запустить конвертер и передать текущий файл.
2.  **Клик снова (С файлом)** → Заменить новым файлом (Одиночный режим).
3.  **Клик снова (Без файла)** → Активировать окно конвертера.

### Установка

DocWen Assistant 2.0 использует DocWen Machine Protocol v1 и единственный контракт Artifact Bundle v2. Версия
исходного кода не подтверждает публикацию; устанавливайте только числовой выпуск, в котором явно указан совместимый
опубликованный выпуск DocWen.

## 🔌 OpenClaw (Plugin + Skill)

OpenClaw 2.0 использует DocWen Machine Protocol v1 и единственный контракт Artifact Bundle v2. Версия исходного
кода не подтверждает публикацию; следуйте странице числового выпуска и устанавливайте его только после успешного
прохождения неизменяемого шлюза публикации.

## ❓ Часто задаваемые вопросы (FAQ)

### Что делать, если конвертация не удалась?

-   Проверьте, не занят ли файл другой программой.
-   Подтвердите, что формат файла правильный.
-   Проверьте в настройках поле «Текущий фактический путь к файлу журнала» или посмотрите журналы ошибок в системном каталоге пользовательских логов; если при проверке пакета используется `DOCWEN_LOG_DIR`, проверьте каталог, заданный этим переопределением.

### Шаблон не отображается?

-   Подтвердите, что файлы шаблонов находятся в каталоге `templates/`.
-   Проверьте, не поврежден ли файл шаблона.
-   Перезапустите программу, чтобы перезагрузить шаблоны.

### Функция корректуры не работает?

-   Подтвердите, что документ в формате .docx или .md.
-   Проверьте, содержит ли документ редактируемый текст.
-   Подтвердите, что правила корректуры включены в настройках.

### Формат вывода не соответствует ожиданиям?

-   Программа генерирует документы на основе стилей шаблона. Чтобы настроить формат вывода, измените определения стилей непосредственно в файле шаблона.
-   Файлы шаблонов находятся в каталоге `templates/`.
-   После изменения стилей шаблона все документы, конвертированные с использованием этого шаблона, будут применять новые стили.

### Ячейки с формулами пусты после конвертации Excel в Markdown?

Это ожидаемое поведение. Программа читает **кэшированные значения** ячеек, а не сами формулы.

**Техническая причина**:
-   В файлах Excel ячейки с формулами хранят как формулу, так и последний вычисленный результат (кэшированное значение).
-   Программа использует режим `data_only=True`, который извлекает только кэшированные значения.
-   Если файл никогда не открывался в Excel (например, был сгенерирован программой) или был отредактирован, но не сохранен повторно, кэшированное значение будет пустым.

**Решение**:
1.  Откройте файл в Excel.
2.  Дождитесь завершения вычисления формул.
3.  Сохраните файл.
4.  Конвертируйте снова.

## 🔒 Функции безопасности

-   **Полностью локальная работа**: По умолчанию обработка выполняется локально и не зависит от онлайн-сервисов.
-   **Защита исходящих соединений зависимостей**: Поддерживаемые точки входа GUI/CLI включают аудит-защиту CPython на весь срок жизни основного процесса Python. Она блокирует любое разрешение DNS/имён и операции AF_INET/AF_INET6 `bind`, `connect`, `connect_ex`, `sendto`, `sendmsg`, сохраняя именованные каналы Windows и сокеты домена Unix.
-   **Явная граница**: Отдельно запущенные процессы, включая Office/WPS/LibreOffice и служебный процесс Office, не контролируются. Это защита от случайного выхода зависимостей в сеть, а не песочница операционной системы.
-   **Нет загрузки данных**: По умолчанию пользовательские файлы не отправляются активно на внешние серверы.
-   **Строгий режим безопасности**: включен по умолчанию; приложение завершается, если не проходят ключевые проверки безопасности. См. [Troubleshooting](../maintenance/troubleshooting.md).

## 📜 Лицензия

Этот проект лицензирован под **GNU Affero General Public License v3.0 (AGPL-3.0)**.

[![License: AGPL v3](https://img.shields.io/badge/License-AGPL%20v3-blue.svg)](https://www.gnu.org/licenses/agpl-3.0)

-   Этот проект использует PyMuPDF (лицензирован под AGPL-3.0), поэтому весь проект также лицензирован под AGPL-3.0.
- В текущей GUI на поддерживаемых путях хоста может использоваться `PySide6-Fluent-Widgets` (QFluentWidgets); эта зависимость распространяется по модели двойного лицензирования `GPLv3 / коммерческая лицензия`, а сам репозиторий по-прежнему распространяется по AGPL.
-   Вы можете свободно использовать, изменять и распространять это программное обеспечение.
-   Если вы изменяете это программное обеспечение и предоставляете услуги через сеть, вы должны предоставить измененный исходный код пользователям.
-   Подробную информацию о лицензии см. в файле [LICENSE](../../LICENSE).
- Сведения о сторонних компонентах приведены в [LICENSE_THIRD_PARTY.txt](../../LICENSE_THIRD_PARTY.txt); краткая сводка распространения находится в [NOTICE.txt](../../NOTICE.txt).

### Контакт

-   **GitHub**: https://github.com/ZHYX91/docwen
-   **Связаться с автором**: zhengyx91@hotmail.com

---

**Автор**: ZhengYX
